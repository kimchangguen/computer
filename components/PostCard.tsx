import Link from "next/link";import {categories,type Post} from "@/data/posts";
export function PostCard({post}:{post:Post}){return <article className="post-card"><Link href={`/blog/${post.slug}`} className="post-image tone-display">{post.featuredImage?<img src={post.featuredImage} alt={post.featuredImageAlt||post.title}/>:<span>{categories[post.category].icon}</span>}<i>TECH NOTE</i></Link><div className="post-body"><span className="category-label">{categories[post.category].name}</span><h3><Link href={`/blog/${post.slug}`}>{post.title}</Link></h3><p>{post.excerpt}</p><div className="post-meta"><time>{post.publishedAt.replaceAll("-",".")}</time><Link href={`/blog/${post.slug}`}>자세히 보기 →</Link></div></div></article>}
export function PostGrid({posts:items}:{posts:Post[]}){return <div className="post-grid">{items.map(p=><PostCard post={p} key={p.id}/>)}</div>}
